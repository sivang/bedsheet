# Project Configuration for AgentCore Deployment
# Generated by Bedsheet v0.4.7

[project]
name = "{{ project_name }}"
version = "0.1.0"
description = "{{ config.name }} - Bedsheet agent deployed to AgentCore"
requires-python = ">=3.11"
dependencies = [
    "bedsheet>=0.4.0",
    "boto3>=1.34.0",
    "fastapi>=0.109.0",
    "uvicorn[standard]>=0.27.0",
    "pydantic>=2.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.24.0",
    "httpx>=0.26.0",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
