# {{ config.name }} - Environment Variables
# Generated by: bedsheet deploy --target gcp
#
# QUICK START (uses your existing GCP credentials):
#   1. Copy this file to .env:  cp .env.example .env
#   2. Authenticate:  gcloud auth application-default login
#   3. Run: make dev-ui-local
#   4. Open: http://localhost:8000
#
# No separate API key needed - uses your GCP account's Vertex AI access.

# =============================================================================
# GCP PROJECT CONFIGURATION
# =============================================================================
# These are used for both local development and production deployment.

GOOGLE_CLOUD_PROJECT={{ gcp.project or 'your-project-id' }}
GOOGLE_CLOUD_LOCATION={{ gcp.region or 'europe-west1' }}

# =============================================================================
# AUTHENTICATION
# =============================================================================
# ADK uses Application Default Credentials (ADC) automatically.
# Just run: gcloud auth application-default login
#
# This works for both local development AND Cloud Run deployment.
# No separate API keys needed!
#
# For local development, ADC uses your user credentials.
# For Cloud Run, ADC uses the service account attached to the service.
