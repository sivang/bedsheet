# {{ config.name }} - Terraform Variables Example
# Generated by: bedsheet generate --target aws-terraform
#
# Copy this file to terraform.tfvars and customize:
#   cp terraform.tfvars.example terraform.tfvars

project_name   = "{{ project_name }}"
region         = "{{ aws.region or 'eu-central-1' }}"
bedrock_model  = "{{ aws.bedrock_model or 'eu.anthropic.claude-3-7-sonnet-20250219-v1:0' }}"

{% if agent.is_supervisor and agent.collaborators %}
# Enable delegate action for supervisor agents to improve trace visibility.
# This provides better observability of supervisor reasoning in traces and Debug UI.
# Can be disabled to use pure Bedrock native collaboration when AWS improves traces.
# Default: true
# enable_delegate_for_supervisors = {{ aws.enable_delegate_for_supervisors | lower }}
{% endif %}
