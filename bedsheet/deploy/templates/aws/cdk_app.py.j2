#!/usr/bin/env python3
"""
{{ config.name }} - AWS CDK Application
Generated by: bedsheet deploy --target aws
"""
import aws_cdk as cdk

from stacks.agent_stack import AgentStack


app = cdk.App()

AgentStack(
    app,
    "{{ project_name | title | replace('_', '') }}Stack",
    env=cdk.Environment(
        region="{{ aws.region or 'eu-central-1' }}",
    ),
)

app.synth()
