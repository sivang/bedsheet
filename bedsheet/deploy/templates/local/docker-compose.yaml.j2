version: '3.8'

services:
  {{ project_name }}:
    build: .
    ports:
      - "{{ local.port }}:{{ local.port }}"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    {% if local.hot_reload %}
    volumes:
      - .:/app
    {% endif %}
