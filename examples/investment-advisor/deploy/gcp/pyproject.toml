# investment-advisor - GCP Deployment
# Generated by: bedsheet generate --target gcp
#
# This is a deployment project, not a distributable package.
# Use: uv run python -m google.adk.cli web .

[project]
name = "investment-advisor"
version = "0.2.0"
description = "Investment Advisor - Multi-Agent System with REAL DATA on GCP"
requires-python = ">=3.11"
dependencies = [
    # Google ADK
    "google-adk>=0.1.0",
    # Google AI SDK (with pinned aiohttp for compatibility)
    "google-genai>=1.0.0",
    "aiohttp>=3.10.0",
    # REAL DATA tools
    "yfinance>=0.2.40",
    "ddgs>=6.0.0",
]