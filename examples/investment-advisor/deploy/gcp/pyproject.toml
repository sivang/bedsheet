# investment-advisor - GCP Deployment
# Generated by: bedsheet generate --target gcp

[project]
name = "investment-advisor"
version = "0.1.0"
description = "A Bedsheet agent deployment on GCP"
requires-python = ">=3.11"
dependencies = [
    # Google ADK
    "google-adk>=0.1.0",
    # Google AI SDK
    "google-genai>=1.0.0",
    # Bedsheet
    "bedsheet>=0.4.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"